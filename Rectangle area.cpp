#include<stdio.h>
int main()
{
	int length=4, breadth=5, area=length*breadth;
	printf("the lenght and breadth of rectangle is %d and %d\n", length, breadth);
	printf("area of the rectangle is %d",area);
	return 0;
	
	
	
}
